# 二战风云(ezfy) 复刻 —— 待完成功能清单

> 参照对象:本地原始服务 `http://192.168.0.104/ezfy/cityHome`(源码 `D:\mxz-code\gitee\stzb-fk`,SpringBoot + Thymeleaf)
> 复刻对象:`qqjiayuan`(Go + Gin + GORM / Vue 2.7),入口 `http://localhost:8000/#/games/ezfy`
> 比对方式:原始源码 28 个 Controller 路由 + 84 个 Thymeleaf 模板 ⇄ 本项目 `Ezfy.vue` 页面块 + `server/internal/handler/ezfy*.go` 接口
> 页面基准:`二战风云/templates/`(51 个原版模板快照)
> 更新日期:2026-09-19(第八轮)

---

## 一、已完成

### 1.1 军官 / 学院系统(2026-09-18 第一轮)

首页「军官」原先只弹「暂未开放」,已按原版完整复刻(后端 + 前端 + 种子数据)。

| # | 功能 | 复刻实现 | 状态 |
|---|------|---------|------|
| 1 | 名将配置(31 名) | `ezfy_cfg_general`,数据源自 `inithebing.sql` | ✅ |
| 2 | 技能配置(15 个) | `ezfy_cfg_skill` | ✅ |
| 3 | 装备配置(26 件,含 8 颗地形珠宝) | `ezfy_cfg_equipment` | ✅ |
| 4 | 军校招募(候选=军校等级,每日刷新限 5 次) | `GET/POST /api/games/ezfy/acade/recruit*`,`ezfy_recruit` 表 | ✅ |
| 5 | 参谋部容量(军官上限=参谋部等级) | 招募/收编均校验 | ✅ |
| 6 | 军官列表 / 详情 | `GET /officers`、`GET /officers/:id` | ✅ |
| 7 | 招募费用(名将等级 × 500 黄金)、同将唯一 | 跨城防重复招募 | ✅ |
| 8 | 赏赐(1 万金 → 忠诚 +10) | `POST /officers/:id/grant` | ✅ |
| 9 | 技能学习(1 万金/个,最多 3 个) / 遗忘(免费) | `POST /officers/:id/skill` | ✅ |
| 10 | 装备穿戴 / 卸下(同部位唯一,珠宝不限,等级门槛) | `POST /officers/:id/equip` | ✅ |
| 11 | 任命市长 / 城守 / 卸任(同职位唯一) | `POST /officers/:id/position` | ✅ |
| 12 | 俘虏收编 / 释放 / 流放 | `POST /officers/:id/captive`、`/exile` | ✅ |
| 13 | 野地/寇城掉宝(按等级掉装备 + 按地形掉珠宝) | 战胜结算时入库 `ezfy_equipment` | ✅ |
| 14 | 战胜寇城/高级野地俘虏守将 | 20%/40% 概率,星级 1-4,忠诚 30 | ✅ |
| 15 | 市长产量加成(产量 +10% + 后勤/20) | 已接入 `calcResource` | ✅ |
| 16 | 前端 6 个子页:军官 / 计谋 / 招募 / 任命市长 / 装备 / 技能 | `Ezfy.vue` 内 `cur === 'acade'` + `.acade-tab` | ✅ |
| 17 | 军官详情页 | `cur === 'officerdetail'` | ✅ |

数据表:`ezfy_cfg_general` / `ezfy_cfg_skill` / `ezfy_cfg_equipment` / `ezfy_officer` / `ezfy_equipment` / `ezfy_recruit`

### 1.2 出征带将 + 军官入战斗(2026-09-18 第二轮)

军官从「只能看」变成「真能打」。

| # | 功能 | 复刻实现 | 状态 |
|---|------|---------|------|
| A1 | 出征带将(选将) | `GET /officers/onduty` 列出在职军官;出征页下拉选将;`createOrder` 校验(不存在/出征中/俘虏均拦截);派遣(7)必须带将;同盟运输不能带将 | ✅ |
| A2 | 军官属性入战斗 | 攻击加成 = 军事 + 装备军事 + 技能;科技项补齐弹道学/重工;战报与战斗详情均显示军官行 | ✅ |
| A3 | 军官技能入战斗 | 突击+10 鼓舞+5 爆破/空袭/海战/装甲突击+8 攻击;攻速/移速+10 速度;掠夺+10% 战利品;修养+10% 伤兵恢复 | ✅ |
| A4 | 城守守城加成 | 守城防御 +10,防御/掩体+10、生命/鼓舞+5 | ✅ |
| A5 | 出征扣忠诚(-5)、归零离职发战报 | `officerGoOut` | ✅ |
| A6 | 军官战斗获经验升级(等级×200,随机 +2 属性) | 胜利时 `exp = 我方战损/10 + 50`,`addOfficerExp` | ✅ |
| A7 | 军官随军调任(增援后调往目标城市) | `moveOfficerTo` | ✅ |
| A8 | 雷达预警补充统帅/出发时间分级 | 雷达 4 级显示统帅、6 级显示出发时间 | ✅ |

### 1.3 仓库保护资源 + 军队动态详情(2026-09-18 第三轮)

> 说明:这两项在原 Java 工程里其实**都没真正实现** —— `任务.md` 把「仓库保护资源」标为「未完成」
> (仓库建筑存在但掠夺未用保护),`viewCityTroopOut.html` 则是一页硬编码样例数据。
> 本轮按设计文档《二战风云建筑升级一览.txt》把它们**真正做出来**。

| # | 功能 | 实现 | 状态 |
|---|------|------|------|
| D1 | 仓库保护资源 | 仓库(建筑12)1-10 级保护总量 `10000/22000/49500/88000/137500/198000/269500/352000/445500/550000`(源自设计文档);掠夺玩家城市时先扣保护额度,只掠超出部分;战报与「被掠夺报告」均标注保护明细 | ✅ |
| D1b | 仓库资源配比调配 | 「仓库调配」页可设 4 项资源保护占比(合计≤100%,未分配部分不产生保护);`ezfy_city` 加 `ware_food/steel/oil/rare` 四列,默认各 25 | ✅ |
| D2 | 军队动态详情 | 命令详情页按原版 `report/viewCityTroopOut` 结构重做:出发地 / 目的地 / 命令 / 军官 / 统帅 / 状态 / 出发时间 / 到达时间 / 返航时间 / 耗油 / 【进攻方军队】/ 军队携带资源,底部给「指挥(司令部)」「召回」「采集」「查看战报」入口 | ✅ |

实测(目标城各资源 36000、仓库 5 级、保护线 34375):掠夺比例 10% 本应掠 3600/项,
保护后实际只掠 **1625**/项,目标资源精确停在 **34375**,战报输出
`目标仓库保护了部分资源: 粮护34375 钢护34375 油护34375 稀护34375 (合计少掠7900)`。

### 1.4 社交三件套(2026-09-18 第四轮)

| # | 功能 | 复刻实现 | 状态 |
|---|------|---------|------|
| B1 | 好友搜索 / 添加 | 新增 `GET /api/friends/search?keyword=`(按家园号码精确/前缀或昵称模糊,排除自己与已拉黑,带「已是好友/已申请」标记);前端好友页加搜索框、结果表、`[加好友]`(可填验证信息)、好友列表加 `[私聊]` | ✅ |
| B2 | 聊天频道 | `EzfyChat` 加 `channel`(1公共 2军团 4系统)/`talk_type`;`GET /chat?channel=` 分频道返回,军团频道复用军团聊天、无军团时自动降级公共,系统频道返回系统公告+系统消息且只读;前端聊天页加 `公共\|军团\|系统\|私聊` 切换条,发言限 25 字 | ✅ |
| B3 | 联络中心(盟友驻军) | 按原版 `liaisonIndex` 文案实现:联络中心(建筑15)**1级可加入联盟、2级可创建联盟**(消耗 5 万黄金,原版 50 钻石按项目惯例折黄金);**每级 +10 人联盟人数上限、+1 支盟友驻军**;`GET /liaison` 返回等级/联盟/成员数上限/驻军列表;增援(6)放开到同联盟成员城市,受目标城联络中心等级限制;盟友驻军时军官留本城不调任 | ✅ |
| — | 补齐可建造建筑 | 军事区新增 **联络中心(15)/轻工厂(16)/重工厂(17)** —— 原版建筑表有、本项目 UI 之前漏掉 | ✅ |

实测:联络中心 2 级 → 创建联盟成功(成员上限 2×10=20)、驻军上限 2;
向联盟成员城市 3(联络中心 1 级)增援成功并常驻,再派第二支被拒
`目标城市联络中心1级, 最多接收1支盟友驻军`;向非联盟城市增援被拒。

### 1.5 军官类道具 + 批量使用/购买(2026-09-18 第五轮)

设计依据:`参考材料/开发文档/QQ家园二战风云.txt` 道具段(#7 招生简章 / #8 经验书 / #9 将领宝箱 / #10 野地珠宝 / #11 寇城掉落)。
原工程这批标为「未实现」,本轮补齐。

| # | 道具 | 效果 | 状态 |
|---|------|------|------|
| C2-1 | **招生简章**(id 13) | 立即刷新军校候选名将,**不占用每日 5 次刷新额度** | ✅ |
| C2-2 | **经验书**(id 14) | 指定军官获得 1000 经验(可批量,会正常触发升级与属性成长) | ✅ |
| C2-3 | **军官技能书**(id 15) | 指定军官**免费**学习 1 个技能(不消耗黄金) | ✅ |
| C2-4 | **重修书**(id 16) | 重置军官属性成长(回到该名将初始值)并清空已学技能,**等级与经验保留** | ✅ |
| C1 | 批量使用 | `useItem` 支持 `count`(1-99);**单次生效类道具**(图纸/技能书/重修书)强制为 1;中途失败返回「已使用 N 个后中断」 | ✅ |
| C1 | 批量购买 | `POST /mall/buy` 的 `count` 落地到前端:数量输入 + 实时合计黄金 | ✅ |

前端:背包页点 `[使用]` 展开面板(数量 / 全选 / 军官下拉 / 技能下拉),商城页点 `[购买]` 展开数量与合计。

实测:经验书×5 → 巴顿 Lv1→Lv7、属性随机成长;技能书学「防御」成功;重修书把属性重置回
名将初始 220/60/90 且技能清空、等级保留;技能书 `count=2` 被拒;超量使用被拒。

### 1.6 顺带修掉的缺陷

| 缺陷 | 说明 |
|------|------|
| **战斗引擎移动判定错误(严重)** | `ezfy_battle.go` 里写成 `moveMap[unit.cfg.ID] > 0`,Go 缺键取零值 → 未配置的兵种被当成「停止」。原版是 `getOrDefault(id, 1)`(**缺省前进**)。后果:攻方近战兵种原地不动、永远进不了射程 → **任何没配过司令部的玩家出击必败**。已按原版语义修正为「缺键=前进」,并回归验证「显式配停止」仍然生效 |
| **出征后跳到空白页** | `doOrder` 成功后会 `cur = 'orders'`,但模板里**没有** `cur === 'orders'` 分支 → 出征完看到一片空白。已补「出征队列」页(类型/目标/统帅/状态/详情/召回),地图工具栏也加了入口 |
| **「军情」页整页渲染失败** | 模板里用了 `reports`,但 `data()` 里从未声明 → Vue 报 `Property or method "reports" is not defined`,整页白屏。已补 `reports: []` |
| **时间全部显示成 `NaN-NaN NaN:NaN`** | `fmtTime()` 把 ISO8601 字符串里的 `-` 换成 `/` 后交给 `new Date()`,得到 Invalid Date。后端返回的是 `2026-09-18T21:15:25.054+08:00`,直接 `new Date(t)` 即可。已修,并按要求精确到时分秒 |
| **建筑列表 Vue 重复 key 警告** | 军事区 `:key="'zb' + (b.id || b.bid)"` —— 已建建筑的**记录 id** 和可建池的**建筑 id** 共用同一命名空间,数值撞上就报 `Duplicate keys detected: 'zb17'`。已改为 `zb-b{记录id}` / `zb-p{建筑id}` 两套前缀 |

---

## 二、第六轮:页面纠错 + 补齐(2026-09-19)

> 本轮起因:「有些页面不对、建筑也不对」。做法是把 `二战风云/templates/` 的 51 个原版模板
> 逐页与本项目 `Ezfy.vue` 的分支比对,先修**错**的,再补**缺**的。

### 2.1 严重缺陷修复

| # | 缺陷 | 根因 | 修复 |
|---|------|------|------|
| **X1** | **军事区/资源区建筑归属完全错** | 前端 `zoneBuildings` 硬编码了错误的建筑 id 分区:军事区只有 11 种(漏了市政厅/民居/军校/参谋部/交易所/仓库),资源区错把民居、仓库也算进去 | 按原版 `BuildingController.militaryIndexInfo`(`type∈{2,3,4}`)与 `sourceIndex`(`type==1`)改为**按 type 分区**;军事区 17 种、资源区 4 种 |
| **X2** | **可建造池前端硬编码,与配置漂移** | 前端手写 `pool` 数组,与 `ezfy_cfg_building_level` 的真实造价/时间不同步 | 新增后端 `buildPool()`(复刻原版 `buildList`),`/view` 下发 `building_pool`,前端不再硬编码;建造资格(军工厂≤5、民居≤10、其余唯一、航海协会仅沿海)由后端统一判定 |
| **X3** | **配置表种子永不更新 → 库里配置是错的** | `seedEzfy` 用 `count>0 就跳过`,导致 `ezfy_cfg_gen.go` 修正过的配置永远落不到已有库 | 改为按主键 **upsert**(`OnConflict{UpdateAll}`),配置以 `inithebing.sql` 转换结果为唯一事实来源 |
| **X4** | **GORM 零值陷阱:市政厅可被拆除** | `CanDelete int` 的 tag 写了 `default:1`,Go 零值 0 被当作「未设置」而套用库默认值 1,市政厅的 `can_delete=0` 永远存不进去 | 去掉 `default:1`;同类问题的 `WareFood/Steel/Oil/Rare`(0%=不保护)、`Feelings`(0民心)、`TaxRate`(0%税率)、`AtkMove/DefMove`(0=停止)一并去掉非零默认值,并在建城处显式赋初值 |
| **X5** | **司令部页整页渲染报错** | `go('hq')` 并发调 `loadTroops()`/`loadTargets()`,后者依赖前者的 `troopsData.cfgs`,兵种数据未到时 `targetCfg[id]` 为 undefined → 模板 `targetCfg[t.id].atk` 抛错 | `loadTroops` 返回 Promise 并串联;新增 `ensureTargetCfg()` 按兵种表补齐默认键;`loadTargets` 改为「先建全默认值再覆盖」 |
| **X6** | 建筑列表「一键满级」文案 | 显示为 `[满级]` | 按原版改为 `[一键9级]`(即 `max_level-1`) |

### 2.2 页面纠错

| # | 页面 | 原版(`二战风云/templates/`) | 本轮修复 |
|---|------|---------------------------|---------|
| P1 | **计谋页**(acade/scheme) | 12 条计谋说明(恫疑虚喝/隐真示假/十面埋伏/欲擒故纵/偷梁换柱/反客为主/先发制人/未雨绸缪/虚实相乱/调虎离山/各个击破/舍车保帅),各标注所需信号弹 4~12 | 原先错误地显示「名将图鉴」;已改为真正的 12 条计谋,**名将图鉴独立成 tab** |
| P2 | **战俘营**(acade/conquer) | 参谋部页头 `参谋部(N级) [去招募] | 战俘营`,表格 姓名/等级/星级/后-军-学/费用/招募 | 原先**整页缺失**;已新增 tab,复用 `/officers` 的 `is_captive` 数据,支持 [雇佣]/[释放] |
| P3 | **军官卡片** | 含 攻/防 两项 | 后端 `/officers` 补 `attack`(`officerBattleBonus`)/`defence`(`officerGuardBonus`)字段,列表补 攻/防 列 |
| P4 | **地图页**(map/index) | `输入坐标查找`(x/y + [查找])、`收藏列表`、`当前坐标中心:(x,y)`、方向按钮为 `向上/向右/向下/向左/回到本城` | 全部补齐;新增 `ezfy_map_star` 表与 3 个接口实现真实的坐标收藏;方向语义按原版(向上=x-R,向右=y+R,向下=x+R,向左=y-R) |
| P5 | **出征页**(map/mapAreaOrders) | `集结令` 下拉、18 兵种带 `0~上限` 提示、**携带资源对任何命令都显示**、`宿营`(时/分,≤24h)、`[计算]` 显示 油耗/负重/耗时 | 全部补齐:资源携带常显(请求体同时带 troops+resources)、兵种输入带 max/placeholder、宿营落地(新增 `ezfy_order.wait_min`,返航时间顺延)、新增 `POST /order/preview` 预算接口驱动 [计算]。`集结令` 原版 `任务.md` 标为「未实现」,页面保留该行但标为暂未开放 |
| P6 | **出征队列**(report/index) | 列:命令/目标/状态/军官/**抵达时间** | 补「抵达时间」列;`/orders` 补 `arrive_text`/`start_text`/`return_text`/`wait_min`/`status_name` |
| P7 | **聊天**(chat/chatIndex) | `每次发言消耗一个喇叭(最大25个字)`、`[频道] 时间 名字 说: 内容`(名字可点进个人页)、**30 秒发言冷却** | 文案与结构对齐;名字链到 `/user/{id}`;前后端都加了 30 秒冷却(服务端兜底,前端倒计时) |
| P8 | **科技页**(tech/techIndex) | `【科技中心】:N级`、研究中显示 `[加速][取消]` | 标题对齐;新增 `POST /techs/cancel`(取消研究并全额退还本次消耗) |
| P9 | **统帅信息页**(user/info) | 账号/昵称/阵营/声望/军衔/城市数/人口数/军官数/ID/等级/特权/VIP等级/经验/状态/申请好友 + 顶部 资源·军官·军队·科技·城防·个人 子导航 | 全部补齐;`/view` 补 `account`/`user_level`/`user_exp`/`officer_count`。原版的「旗号/手机号/改名表单」在本项目家园侧无对应字段,未做 |
| P10 | **资源详情**(resource/resourceA) | `现有/产量/容量` + 资源说明文字 | 补 5 种资源的说明文案(黄金一段直接取原版原文) |

### 2.3 新功能(原版页面有、后端没实现的)

| # | 功能 | 原版位置 | 说明 |
|---|------|---------|------|
| F4 | **城市迁移** | `city/cityHallMove.html` | 原版只实现了「20 万黄金 + 随机空位」一种,区域/指定坐标/沿海三个分支模板里有、后端没有。本项目按页面语义补全三种:`low` 迁城计划(选 10 个区域之一随机空平原)、`high` 高级迁城计划(指定 x/y,非沿海平原)、`sea` 沿海迁城计划(指定 x/y,沿海平原),均 20 万黄金。区域用城市可落点区(50~450)按 5×2 均分,沿用原版 10 个区域名 |
| F1 | **调整生产(开工率)** | `city/sourceSet.html` | 4 项资源开工率 1~100(带 [最小]/[最大]),实际产量 = 基础产量 × 开工率 / 100。`ezfy_city` 加 `rate_food/rate_steel/rate_oil/rate_rare` 四列。注:0 与「未设置」在库里不可区分,故把 0 归一为 100%,输入下限取 1 |
| — | 市政厅入口补齐 | `city/cityHall.html` | 原版 8 个入口,补上 [调整生产]、[地图搬迁],并把 [附属野地] 文案改为原版的 [占领野地] |

### 2.4 本轮验证

- 后端 `go vet ./...` 通过;34 个页面逐页渲染,前端错误 **0**
- 建筑区:军事区 7 已建 + 11 可建(市政厅不再出现 [拆除])、资源区 农田 + 3 可建
- 迁城实测:(438,264) → 西欧区域 (92,124),黄金 -200000;指定 (100,100) 被正确拒绝(`该坐标是沿海平原, 请使用沿海迁城计划`)
- 开工率实测:设 80/90/100/50 后读回一致
- 出征实测:前端同款请求体提交侦查成功,队列返回 `抵达时间 2026-09-19 10:10:54`
- [计算] 实测:步兵×10 → `油耗:1 /负重:500 / 耗时:28分(单程14分)`

### 2.5 遗留 / 已知问题

| # | 问题 | 说明 |
|---|------|------|
| Y1 | **世界坐标与「大陆」不匹配** | `ezfyContinentOf` 只在 0~150 范围内定义陆地,而城市落点范围是 50~450,导致绝大多数城市显示为「大海」。属既有设计遗留,本轮未动 |
| Y2 | `server/internal/seed/seed.go` 的 `noble_plans.limit` 报错 | 幂等判断把反引号写进了 `HasColumn` 列名,每次启动报 `Error 1060 Duplicate column name 'limit'`(无害) |
| Y3 | 集结令 | 原版 `任务.md` 标为未实现,本轮页面保留占位未实现 |
| Y4 | 旗号 / 手机号 / 改名卡 / 旗帜 | 家园侧无对应字段,统帅信息页未做 |

---

## 三、第七轮:建造规则修正 + 野地类型 + 节日活动(2026-09-19)

### 3.1 建造规则修正(用户反馈)

| # | 问题 | 原版规则 | 修复 |
|---|------|---------|------|
| **B1** | 资源区建了一种资源建筑后,该种就再也建不了 | `BuildingController.buildList` 资源区分支是 `can = true` —— **农田/炼钢厂/石油基地/稀矿厂可重复建造**,只受建筑总数上限约束;`buildBuilding` 里 type 1 也不走唯一性校验 | `buildPool` 对 `type == 1` 直接 `can = true`;池子里补 `built_count` 显示「已建N个」 |
| B2 | 军事区民居/军工厂可重复,其余唯一 | 军工厂 ≤5、民居 ≤10,`type 2/3` 每类限 1 个 | 原本已正确,本轮验证通过(农田重复建 ✓、民居重复建 ✓、科研中心重复建被拒 ✓) |

实测:资源区连续建 3 块农田成功;科研中心第二次被拒「该建筑已存在」。

### 3.2 野地类型(用户反馈「野地不对,有类型,现在都叫野地」)

原版 `MapController.TERRAIN_NAMES = {"", "平原", "草原", "森林", "盆地", "丘陵", "沼泽", "山地", "海洋"}`,
地形 id 就是 `h % 8 + 1`。本项目此前地图格子一律显示「野N」,附属野地列表也一律写「陆地野地」。

修复:
- 新增 `ezfyTerrainName(t)`(与 `ezfyTerrain` 的哈希同源,已逐条核对原版)
- 地图格子改为 `地形名+等级`(如 `盆地7`、`沼泽10`),海洋显示 `海N`,城市/寇城/废墟保持 `城/寇N/墟`
- 地图接口 `MapView` 每个 cell 补 `terrain_name`;选中格详情、`WildlandView` 也补
- 附属野地列表的「类型」列改为显示地形名(`w.terrain_name`),海野额外标 `(海野)`
- 地图图例改为「城=城市 寇=寇城 墟=废墟 海=海洋 数字=等级 / 陆地野地按地形分: 平原/草原/森林/盆地/丘陵/沼泽/山地」

### 3.3 返回按钮样式统一(用户反馈)

`.btn-return` 原来只作用在底部 `.bottom-nav` 里(「返回游戏」),页内的 `[返回首页]` 等是纯文本链接。
改为 `.ezfy-page a.btn-return` 全局样式,并把页内 36 个 `[返回首页]/[返回市政厅]/[返回地图]/[返回出征队列]/[返回军官]`
都挂上同一个类 —— 两者现在完全同款(深蓝底、白粗体、圆角)。

### 3.4 节日活动(E1,`待完成功能.md` 原定下一阶段)

设计依据 `参考材料/开发文档/福利.txt`(26 条活动点子),原版 Java 只做了静态说明页。

| 项 | 实现 |
|---|------|
| 数据表 | `ezfy_activity`(name/type/param/start_time/end_time/status/des) |
| 5 种类型 | 1 资源增产 2 造兵打折 3 建造加速 4 研究加速 5 声望加成 |
| 生效判定 | `status=1` 且 `start_time <= now < end_time`;同类多个取加成最大的 |
| 接入点 | 增产 → `calcResource` + `getResourceCalc`;造兵打折 → `trainTroop` 扣费 + `/troops` 列表展示;建造加速 → `buildBuilding`/`upgradeBuilding`;研究加速 → `researchTech`;声望加成 → `addPrestige`(覆盖所有声望来源) |
| 接口 | `GET /games/ezfy/activity` |
| 管理端 | 「数据管理 → 节日活动」可增删改 |
| 种子 | 丰收节/军工动员/建设狂潮/科技峰会/荣耀之战,默认全部**未开启**(避免上线即改数值) |

实测(逐个开启再关闭):

| 活动 | 验证 |
|------|------|
| 丰收节 +20% | 农田产量 `base=100 bonus=-8` → `bonus=10`(100×1.2×民心0.92=110) |
| 军工动员 -25% | 步兵消耗 粮50/钢70 → 粮37/钢52,`train_discount=25` |
| 建设狂潮 -30% | 农田建造耗时 45.0秒 → 31.5秒 |

### 3.5 顺带修掉

| 缺陷 | 说明 |
|------|------|
| **产量计算有两份拷贝** | `calcResource`(真实结算)与 `getResourceCalc`(详情展示)各写了一遍产量公式。加节日活动时只改了前者,导致详情页数字不动。已在后者补上,并把「造兵打折」抽成 `trainCostWithActivity()` 供扣费与展示共用 |

### 3.6 遗留

| # | 问题 | 说明 |
|---|------|------|
| Y5 | 产量公式仍是两份 | 本轮只是把两份对齐,没有真正合并;后续再动产量逻辑务必两处同改,或做一次重构 |
| Y1/Y2/Y3/Y4 | 见 2.5 节 | 世界坐标与「大陆」不匹配、`seed.go` 反引号报错、集结令、旗号/手机号 |

---

## 四、第八轮:战报修复 + 地图/出征/野地页面对齐(2026-09-19)

### 4.1 战报打不开(严重)

| 缺陷 | 根因 | 修复 |
|------|------|------|
| **点战报毫无反应** | 模板里写着 `@click="openReport(r)"`,但 `openReport` 这个方法**从未定义过** → Vue 抛 `openReport is not a function`,点击静默失败 | 补 `openReport(r)`:拉 `/reports/:id` 详情、置 `curReport`、把列表项标为已读;接口异常时降级用列表数据 |

实测:点「侦查报告: 野地3级」→ 正文正常显示(公文报告 / 守军情况 / 侦查完成)。

### 4.2 军官招募「失败」

排查结论:**不是 bug**。API 与前端链路都通,失败信息是 `参谋部容量不足(参谋部3级容纳3名军官)` ——
原版 `recruitOfficer` 也是「参谋部等级 = 军官上限」,本项目与之完全一致。
真正的体验问题是**点了才知道满了**。已改进:

- 招募页容量已满时,顶部显示红字提示,并给出 [升级参谋部] / [军官] 两条出路
- 每个候选的 [招募] 在满员时替换为 `(容量已满)`,不再让人白点
- 新增 `officerFull` 计算属性,招募前先拦一道

### 4.3 页面按参考模板对齐

| # | 页面 | 参考 | 改动 |
|---|------|------|------|
| M1 | **地图**(`cur='map'`) | `map/index.html` | 去掉「在地图上直接选出征命令」;补 `发现精英中立城市：[寇(x,y)]`(后端由近及远扫圈取最近寇城);格子文案改为 `地形名+等级`;补 `当前坐标中心:(x,y)` 与图例 |
| M2 | **野地/目标详情**(新 `cur='wildview'`) | `map/mapView.html` | 点地图格子 → 进详情页,结构照原版:`所属区域` / `LvN(x,y) [收藏][收藏列表]` / `地形名(等级)` / 地形产出说明 / `采集可获得：<珠宝>` / `【归属: xxx】` / 守军情况 / 掠夺资源约 / `侦查 掠夺 征服`(城市另有运输/增援, 已占领野地另有采集) |
| M3 | **军工厂**(新 `cur='factory'`) | `city/cityFactory.html` | 点军工厂 → `军事区 ->军营(军工厂)(N级)：` / `正在训练：` / 逐兵种 `兵种名 : 数量 [训练]`;建筑页里军工厂的入口从「训练」改为「军工厂」 |
| M4 | **出征页**(`cur='orderpre'`) | `map/mapAreaOrders.html` | 兵种输入从「只列已有部队」改为**列出全部 16 个可出征兵种**,`placeholder=0~已有数量`、数量为 0 时禁用;出征命令由详情页的 [侦查]/[掠夺]/[征服] 直接带入 |

### 4.4 数据来源说明

- 地形名:`MapController.TERRAIN_NAMES`,已与 `getTerrain` 哈希逐条核对
- 珠宝:`cfg_equipment` 的 `珠宝(地形)` 8 件(id 19-26),地形 1-8 一一对应(平原红宝石/丘陵蓝宝石/森林绿宝石/草原翡翠/沼泽紫水晶/盆地猫眼石/山地玛瑙/海洋珍珠)
- 「某地形可产出某资源」原版**没有**这个区分(Java 里陆地野地一律产 4 种、海野产油稀金)。
  本项目照实显示为「X中可以产出粮食、钢铁、石油、稀矿」,没有照抄模板里那句与实际机制不符的文案

### 4.5 本轮验证

- `go vet ./...` 通过;32 个页面逐页渲染,前端错误 **0**
- 实测链路:地图 → 点格子 → 野地详情(丘陵7 / 紫水晶 / 归属无)→ 点[掠夺] → 出征页(`丘陵(7) (431,257)` / `出征命令：掠夺` / 16 兵种 / 携带资源 / 宿营 / [计算] / [出征])
- 战报:点开显示正文
- 招募:`used=2 cap=3 full=false`;满员时按钮替换为 `(容量已满)`

---

## 五、第九轮:军情页三区改造(2026-09-19,用户反馈)

用户反馈:「军情页面不对,按 `二战风云/templates/report/index.html` 的样式;
要有**军队动态**(出征/采集/派遣/侦查/掠夺都在里面)、**军情警讯**(有人打你)、**战斗报告**(战报查询);
侦察报告要能看到对方**兵力、建筑等级、科技**」。

### 5.1 页面结构(照原版 `report/index.html`)

**渲染效果(与参考模板逐字对齐)**:

```
军情
军队动态 . 军情警讯 . 战斗报告
一键采集 一键收获
命令：掠夺 查看
目标：沼泽(2)(432,259)
状态：出征
军官：巴顿（Patton）
抵达时间：9分58秒
--------------------
```

要点:
- 三个 tab 用 `&nbsp;.&nbsp;` 分隔, 渲染成 `军队动态 . 军情警讯 . 战斗报告`(带空格的点, 与原版一致)
- `一键采集 / 一键收获` **不带方括号**(原版就是裸文本链接)
- 军队动态里的 `查看` 也**不带方括号**(原版 `命令：征服 <a>查看</a>`;战报列表则仍是 `[查看]`,原版本身就不统一)
- **目标** 用「地形名(等级)」而不是「野地N级」—— `ezfyTargetName()` 改为
  `ezfyTerrainName(ezfyTerrain(x,y)) + "(" + level + ")"`,例如 `盆地(5)` / `沼泽(2)`;
  寇城用 `寇城(N)`、特殊目标用 `特殊(N)`、玩家城市用城市名
- **状态** 用游戏口径 `出征 / 采集 / 返回`(原版 `状态：出征`),不再显示 `行军中/驻守中/返航中`


原来军情页只是一个平铺战报列表。现改为顶部三区切换 `军队动态 . 军情警讯 . 战斗报告`:

| 分区 | 内容 | 数据来源 |
|------|------|---------|
| **军队动态** | 所有**在外**的部队(行军中 / 驻守中 / 返航中),每条显示 `命令：X [查看]` / `目标：名(x,y)` / `状态：出征` / `军官：X` / `抵达时间：12分43秒`,尾部 `--------------------` 分隔;顶部带 `[一键采集] [一键收获]` | 新增 `GET /games/ezfy/reports/dynamics` |
| **军情警讯** | **别人打我**:军情警报(雷达预警)、被掠夺报告、城破报告、守卫报告、城市归还、野地丢失 | `GET /reports?category=1` |
| **战斗报告** | **我打别人**:侦查/掠夺/征服报告 + **战报查询**(关键字搜索框) | `GET /reports?category=2&word=` |

点军队动态的 `[查看]` → 进上一轮做的「军队动态详情」页(`cur='orderview'`)。

### 5.2 战报分类修正

`ezfyReportCategory()` 原来把「掠夺报告」漏在「其他」里,且没有「城市归还 / 野地丢失」。现在:

- `1 军情警讯` ← 军情警报 / 被掠夺报告 / 城破报告 / 守卫报告 / 城市归还 / 含「野地丢失」
- `2 战斗报告` ← 侦查报告 / 战斗报告(掠夺·征服共用此前缀) / 征服报告
- `3 其他` ← 采集、运输、增援、派遣、建城、交易、将领变动

### 5.3 新增接口与按钮

| 接口 | 说明 |
|------|------|
| `GET /games/ezfy/reports/dynamics` | 军队动态列表;顺带**先结算**已到点的命令再展示,保证状态是最新的 |
| `POST /games/ezfy/wild/collect-all` | 一键采集:对本城所有**空闲**的已占领野地下发采集命令(每块野地派 1 个城内最弱非城防兵种作采集队);兵不够或司令部队伍数满会跳过并说明 |
| `POST /games/ezfy/wild/harvest-all` | 一键收获:对所有**驻守中**的派遣部队先结算一个采集周期(发资源、可能掉宝物)再召回 |

### 5.4 顺带修掉

| 缺陷 | 说明 |
|------|------|
| **召回/收获的返航时长凭空多 8 小时** | 派遣(7)抵达后会把 `ArriveTime` 推到「下一个结算周期」,而 `RecallOrder` 与收获逻辑都用 `ArriveTime - StartTime` 当单程时长 → 返航时间变成 8 小时+。新增 `ezfyOneWayTravel()`,改用 `(ReturnTime - StartTime)/2`(创建时 `ReturnTime = start + 2*travel`,该式恒等于单程) |
| **`prestigeGain` 作用域越界(编译不过)** | 它声明在 `if win {}` 内,却在 `else` 分支被 `battleStatsTail` 使用。已提到分支外声明 |
| `ezfyDurationText` 重复定义 | 保留 `ezfy_order.go` 里「秒」版,`ezfy_api.go` 的毫秒版删掉,调用处改为传秒 |

### 5.5 侦察报告(已满足用户要求)

`scoutReportBody()` 对**玩家城市**目标会输出(复刻 `参考材料/开发文档/侦察报告1.txt`):

```
资源数量 粮食149848 钢铁150000 石油150000 稀矿150000 黄金1000000
人口100 民心86
建筑等级 市政厅1,民居1,农田10,炼钢厂1,1,1,军校1,仓库5,军工厂1,联络中心1,
城防数量：
军队数量：步兵5000
将领等级：
科技等级：
最后活动时间：2026-09-19 11:01:37
侦查完成。
```

对野地/寇城目标只报「守军情况」(原版也是分两种)。

### 5.6 本轮验证(纯 DOM 断言,不再截图)

- 全站回归:顶部 6 导航 + 首页 18 个入口 **全部 ok**、前端错误 **0**
- 军队动态:一键采集 → 3 条 `命令：采集 / 状态：行军中 / 抵达时间：24秒`
- 一键收获:派遣部队 `驻守中` → 收获并召回 1 支,状态转 `返航中`
- 军情警讯:插入 2 条后 `counts={1:2, 2:5, 3:9}`,分类正确
- 战报查询:`word=野地` 命中 4 条
- 侦查玩家城市:上述完整情报输出

---

## 六、第十轮:地图 / 目标详情 / 出征三页对齐(2026-09-19,用户反馈)

用户给出三个参考模板要求「游戏区布局一样」(明确说明**不复刻导航区与 WAP报时**):
`map/index.html`、`map/mapView.html`、`map/mapAreaOrders.html`。

### 6.1 地图页(`cur='map'`)—— 区块顺序与文案照参考重排

```
新城市(438,264) 切换城市
地图：
输入坐标查找： 收藏列表
横坐标：[input (1~500)]
纵坐标：[input (1~500)] [查找]
发现精英中立城市： [寇(441,261)]
<table> 5×5 格子 </table>
当前坐标中心:(438 , 264)
向上 向右 向下 向左 回到本城
```

| 改动 | 说明 |
|------|------|
| **区块顺序** | 原来「发现精英中立城市」在方向按钮之后,现按参考提到**表格之前**;`地图：` 从 `panel-title` 改为普通行 |
| **横/纵坐标分行** | 原来挤在一行,现各占一行(与参考一致) |
| **5×5 表格** | `mapR` 默认由 7 改为 **2**(→ 5×5);格子从 `<span>` 改为真正的 `<table class="ezfy-map-table">`,列宽自适应、`table-layout:fixed` |
| **翻页步长** | 原来按 `mapR`(=7) 移动,现新增 `mapStep = 2r+1`(=5),与参考的「向上 x-5 / 向右 y+5」一致 |
| **格子文案** | 由 `盆地5` 改为 **`盆地(5)`**;本城格子显示 `城名(x,y)`(参考的 `媒(349,4)`),寇城 `寇(N)`、海 `海(N)`、废墟 `墟` |
| **方向链接去方括号** | `[向上]` → `向上`,与参考的裸文本一致 |
| `[收藏列表]` → `收藏列表` | 同上 |

### 6.2 目标详情页(`cur='wildview'`)

照 `mapView.html`:顶部 `所属区域：X` → `Lv7(431,257) 收藏 收藏列表` → `丘陵(7)` → 产出说明 → `采集可获得：X` → `【归属: 无】` → `侦查 掠夺 征服`。
改动:`[收藏]/[收藏列表]` 去方括号;名称行去掉 `<b>` 加粗(参考是普通文本)。
保留项目自有的「守军情况 / 掠夺资源约」两行(参考模板是硬编码桩数据,没有这两项,但玩家需要)。

### 6.3 出征页(`cur='orderpre'`)

照 `mapAreaOrders.html`:`丘陵(7) (431,257)` 作为普通行(原来用 `panel-title` 标题条);
`出征命令：侦查`;`集结令：0个 [select]`(补上「N个」,新增 `gatherCount` 计算属性从背包取数,原版此道具未实现故恒为 0);
`指挥军官：[select]`;逐兵种 `兵种名:[input placeholder=0~N]`;`携带资源：黄金/粮食/钢铁/石油/稀矿`。

### 6.4 本轮验证(纯 DOM 断言)

- 地图:5 行 × 5 列;第1行 `丘陵(7) | 海(10) | 森林(8) | 沼泽(2) | 平原(9)`
- 翻页:起始 `438,264` → 向上 `433,264` → 向右 `433,269` → 向下 `438,269` → 向左 `438,264` → 回到本城 `438,264`(全部正确)
- 目标详情:`所属区域：大海 / Lv7(431,257) 收藏 收藏列表 / 丘陵(7) / 采集可获得：紫水晶 / 【归属: 无】 / 侦查 掠夺 征服`
- 出征页:`丘陵(7) (431,257) / 出征命令：侦查 / 集结令：0个 / 指挥军官：… / 携带资源：…`
- 全站回归 18/18 ok、前端错误 0

---

## 七、第十一轮:WAP 适配 + 战俘机制 + 聊天标识 + 海城(2026-09-19,用户反馈)

### 7.1 WAP 手机端横向溢出

以 **320px / 360px** 逐页扫描(判据:元素 `getBoundingClientRect().right > 设备宽`)。

**唯一真凶是军官页**:列表 13 列 → 表格 **506px**,把整个文档撑到 516px(视口仅 320)。
地图/好友/军团在 320px 下本来就正常(用户看到的很可能是 8080 那份旧 dist)。

| 修复 | 说明 |
|------|------|
| 军官列表 **13 列 → 8 列** | 名称/星/等级/后·军·学/忠诚/职位/状态/操作;经验、攻、防移进 `[详情]` 页。320px 下表格 300px |
| `word-break: break-word` | 加到 `table th/td`,长名字(如「蒙哥马利（Montgomery）」)能折行,min-content 才会变小 |
| `@media (max-width: 420px)` | 表格字号 15→12、单元格 padding 6→3、`.old-line` 13px、地图格子 10px、input/select `max-width:100%` |
| 兜底 | `.ezfy-page .panel { max-width:100%; overflow-x:auto }` —— 万一还有宽元素,在面板内滚动而不是撑开整页 |

### 7.2 战俘机制(按用户规则重做)

规则:**战俘** ① 打玩家把对方军官**忠诚打成 0** → 弃城归降;② 野地/寇城**配置里有军官**时征服成功可俘获。
**正常军官**来自军校招募。旧实现是「打赢就从可招募名将池随机抽一个」,不看目标有没有军官。

| 改动 | 说明 |
|------|------|
| `captureWildlandOfficer(city, wildType, level, special)` | 重写:`cfg.OfficerMax <= 0` 直接返回空(没军官就没战俘);概率 `20 + OfficerMax/10*5`(上限 60,特殊翻倍);星级随野地等级提高;参谋部容量不足不收 |
| **新增** `defectDefenderOfficers()` | 攻打玩家城后,目标城军官忠诚 **-10~-20**;归零者从原城移除并**转为攻方战俘**(攻方参谋部有空位才收得下);双方各发战报(`将领叛离: X`) |
| 战报分类 | `将领叛离` 归入 **军情警讯** |
| 前端 | 战俘营页补上来源说明 |

### 7.3 聊天「个人 / 同盟 / 系统」标识

新增 `GET /games/ezfy/chat/home`,汇总三来源按时间倒序并打标签:

| 标识 | 来源 |
|------|------|
| **系统** | `talk_type = 0` 的系统消息 |
| **同盟** | 我所在军团的聊天 |
| **个人** | 公共频道的玩家发言 |

首页「世界聊天」预览改用该接口,渲染为 `[个人][昵称]内容`;
聊天页频道条文案同步改为 `个人 / 同盟 / 系统`。

### 7.4 海城 + 海军限制

| 项 | 实现 |
|---|---|
| 建城地形 | 由「只能平原」改为 **平原 或 海洋**;海洋 → **海城**(建城战报写明) |
| `isSeaCity(city)` | `ezfyTerrain(city.X, city.Y) == 8` |
| **海军仅海城可训** | `trainTroop` 拦截 `cfg.Type == 1`(驱逐舰/潜艇/战列舰/航母) |
| 航海协会(19) | **沿海城市 或 海城** 均可建(原来仅沿海) |
| 前端 | `/view` 增加 `is_sea`/`city_kind`;城市列表、城市状态页显示 `[海城]/[陆地城市]`;军队页给海军标注「(海军: 仅海城可训练)」 |

> 「征服完属于自己的野地」**原本已实现**(征服胜利会创建 `ezfy_wildland` 并把地图格子 owner 设为你的城),本轮只确认未改动。

### 7.5 本轮验证(纯 DOM 断言)

- **320px 溢出扫描**:22 个页面全部 `ok`,前端错误 0
- **全站回归**:18 个入口 + 军情三区全部 ok,前端错误 0
- 战俘:忠诚 10 的两名军官被掠夺后归零 → 巴顿进攻方战俘营(loyalty 30),蒙哥马利因攻方参谋部已满而离去
- 聊天:8 条混合消息,`[个人][同盟][系统]` 三种标签齐全
- 海城:陆地城市训海军被拒;海洋(300,45)建城成功并标记海城;非平原非海洋(300,40)建城被拒;海城建航海协会成功

---

## 八、第十三轮:军官模块整体重构(2026-09-19,用户反馈)

用户给出 5 张参考模板,要求军官模块按参考重排,并明确了两条**规则性**改动。

### 8.1 规则改动:招募 = 随机普通军官;名将只能管理端发放

参考 `acade/conquer.html` 的样例 `Jeremy·Lee 26级 1星 31/52/38 26000` —— 这就是**随机生成的普通军官**。

| 改动 | 说明 |
|------|------|
| 新增 `rollOfficerDrafts()` | 30 个西文名 × 30 个姓组合 `First·Last`;等级 `5+rand(军校等级×8)`;星级概率 **5星3%/4星7%/3星20%/2星30%/1星40%**;属性 `20+星×5+rand(25)`;**费用 = 等级 × 1000**(原 500) |
| `ezfy_recruit.candidates` | 由「名将 id CSV」改为 **JSON 草稿数组**(列类型 `varchar(255)` → `text`) |
| 雇佣接口 | `POST /acade/recruit/hire` body `{key}`(原 `/acade/recruit/:id` 传名将 id) |
| 野地俘虏 | `captureWildlandOfficer()` 也改为随机军官,不再从名将池抽 |
| **管理端发放名将** | 新增 `POST /admin/ezfy-players/:id/grant-officer {general_id}` |
| 名将图鉴 | 「获取渠道」统一显示 **管理端发放** |

### 8.2 规则改动:技能换成参考版 12 技能

`skill.html` 的 12 个技能与从 `inithebing.sql` 得到的 15 个**完全不同**,按参考替换为:
尖兵突击 / 弧形防御 / 绝地反击 / 火炮控制 / 坦克突袭 / 四指编队 / 闪电袭击 /
狼群战术 / 越岛战术 / 弹幕支援 / 黄金眼 / 机械改造。

- `EzfyCfgSkill.Des` 由 `varchar(200)` 改 **`text`**(完整说明很长)
- seed 增加 `DELETE FROM ezfy_cfg_skill WHERE id > 12` 清历史遗留行
- **战斗映射同步改**(不改技能就失效):
  - 攻击 `尖兵突击+30` / `火炮控制+10` / `四指编队·狼群战术+15`
  - 防御 `弧形防御+30` / `弹幕支援+10`
  - 速度 新增 `officerSpeedSkill()`:坦克突袭/闪电袭击/越岛战术**任一**即算
  - 伤兵恢复 原「修养」→ **机械改造**;掠夺加成 原「掠夺」→ **黄金眼**

### 8.3 布局(全部按参考模板)

| 页 | 参考 | 关键格式 |
|---|---|---|
| 军官列表 | `acadeIndex.html` | `名字(N级) 查看` / `状态:空闲   评价:N星` / `后勤/军事/学识/忠诚：` / `30/19/58/70` / `攻/防：190/580` / `------------------------` |
| 招募 | `conquer.html` | 表格 `姓名 等级 星级 后/军/学 费用 招募`,动作叫**雇佣** |
| 任命市长 | `setMayor.html` | 保持表格(原版是逐条 `名字(N级)` + `状态：空闲[任命市长]`) |
| 技能 | `skill.html` | **编号列表** `1、尖兵突击:<完整说明>` + `效果：X` |
| 名将图鉴 | `conquer.html` | 列 = `名称 等级 星级 军/后/学 获取渠道 状态` |

### 8.4 本轮验证(390×844 手机视口,纯 DOM 断言)

```
军官列表:  Michael·Murphy(21级) 查看
          状态:空闲   评价:1星
          后勤/军事/学识/忠诚：
          25/33/46/100
          攻/防：33/10
          ------------------------

招募:      Harold·Brown  25级  3星  42/58/46  25000  雇佣
技能:      1、尖兵突击:所有兵种、不含城防。擅于率领部队作为尖兵…（12 条带完整说明）
名将图鉴:  Bradley（布兰得利） 150 5 220/300/215 管理端发放 未拥有
```

- 雇佣实测成功:军官入库、候选里移除、扣费正确
- 全站回归前端错误 0;320px 横向溢出扫描无异常

---

## 九、第十四轮:地图格子改成朴素文字表(2026-09-19,用户反馈)

用户要求地图格子跟 `map/index.html` 一样 —— 参考是**朴素的文字表格**(`<td><a>盆地(5)</a></td>`),
没有底色、没有边框。只改了 `.ezfy-map-table` 的样式:

| 项 | 原来 | 现在 |
|---|---|---|
| 格子底色 / 边框 | 5 种颜色区分(本城/城市/寇城/海/野地) | **全部去掉**(`background:none; border:0`) |
| 对齐 | `text-align: center` | `text-align: left`(参考 `<td>` 默认左对齐) |
| 字号 | 11px,窄屏再压到 10px | 12px,窄屏 **11px** |
| 本城 | 黄底 | 只保留 **加粗 + 红色** |

实测(390px)渲染:

```
寇(8)   盆地(0)  山地(8)  沼泽(9)  平原(2)
海(0)   森林(1)  草原(4)  丘陵(6)  盆地(8)
山地(9) 沼泽(3)  平原(3)  海(1)    森林(0)
沼泽(6) 森林(5)  海(1)    丘陵(0)  草原(0)
山地(1) 盆地(1)  平原(2)  沼泽(10) 森林(9)
```

样式断言 `bg=transparent / border=0px / align=left`;320px 溢出扫描无异常,全站回归前端错误 0。

---

## 十、第十五轮:地图网格错位根因 + 表格样式对齐(2026-09-19,用户反馈)

### 10.1 ★ 根因:前后端网格尺寸不一致 → 整张网格错位

`loadMap()` 调 `/games/ezfy/map` **没带 `r`**,后端用默认半径返回;前端 `mapRows` 却按 `mapR`(=2) 切行。

实测:后端默认 `r=7` → 返回 **225 格(15×15)**,前端按 5 列切 → **45 行**,
于是 `rows[2].cells[2]` 根本不是真正的中心格,**本城也就不在中间**;移动时切片边界跟着变,
看起来就像「本城跟着跑」。

修复(三重保险):
1. `loadMap()` 带上 `?r=` + `this.mapR`
2. 后端默认半径 `7 → 2`(5×5,与参考一致)
3. `mapRows()` 改为**用实际返回的格子数推导边长**:`size = √n`(n 是完全平方数时)

修完实测:初始中心 `328,62` 中心格 = **`新城市(328,62)`** ✓;
向上后中心 `323,62` 中心格 `沼泽(8)`(正常平移);回到本城复原 ✓。

### 10.2 表格样式:「还是表格」的真凶是全局 `td` 虚线下边框

参考 `map/index.html` **没有任何表格 CSS**,就是浏览器默认样式。
而 `.ezfy-page table td` 有 `border-bottom: 1px dotted #ddd`(全局样式),
`.ezfy-map-table td` 没覆盖 border → 每格带虚线,看着就像表格;
再加 `width:100%` + `table-layout:fixed` 把 5 列各撑到 20%,所以「太长了」。

```css
.ezfy-map-table { width:auto; max-width:100%; border-collapse:separate; border-spacing:1px; }
.ezfy-map-table td { padding:0 1px; border:0; text-align:left; white-space:nowrap; }
.ezfy-map-table a { display:inline; margin:0 1px; font-size:14px; text-decoration:none; }
```

窄屏 13px(参考 18px 手机放不下);本城只保留**加粗+红字**。
实测表格宽 285px、`td` 边框 0px、无下划线,320px 下文档宽 = 320(不溢出)。

### 10.3 收藏列表:备注名重复带坐标

`addStar()` 默认备注名是 `山地(331,55)`(已含坐标),列表模板又拼一次 `(x,y)`
→ 显示成 `山地(331,55)(331,55)`。
改为默认名用 `cellText(cell)`(即 `地形名(等级)`),坐标由模板统一拼;并清理库里已有脏数据。

现在显示:`沼泽(10)(329,56)` / `森林(3)(331,59)`。

---

## 十一、第十七轮:精英中立城市跳转 + 地图字号间距(2026-09-19,用户反馈)

### 11.1 「发现精英中立城市」点错行为

原来 `@click="jumpTo(eliteCell.x, eliteCell.y)"` —— 只是把地图挪过去(等于刷新地图)。

改成 `openElite()`:构造 `{x, y, area_type:2, name:'寇城'}` 调 `openCell()`,
**直接进该寇城的目标详情页**(那里就是 侦查/掠夺/征服 的出征入口)。

实测点 `[寇(321,55)]` 后进入详情页:

```
Lv8(321,55) 收藏 收藏列表
寇城(8)
寇城中囤积了大量资源与宝物
【归属: 无】
守军情况：摩托化骑兵约45000-45000
侦查  掠夺  征服
```

参考 `map/index.html` 里这个链接也是指向 `map/view/...` 详情页,不是刷新地图。

### 11.2 地图字号 / 间距

| 项 | 原来 | 现在 |
|---|---|---|
| 字号 | 14px(窄屏 13px) | **与正文 `.old-line` 一致**:16px / 窄屏 13px |
| 格子间隔 | `border-spacing: 1px` | **`6px 2px`**(窄屏 `4px 2px`),`td` padding 归零 |
| 上边距 | 4px | 6px |

实测 320px:地图字号 13px = 正文字号 13px,间距 `4px 2px`,表宽 283px,docW = 320(不溢出)。

---

## 十二、第十八轮:表格默认朴素 + 军官导航字号 + 私聊(2026-09-19,用户反馈)

### 12.1 表格「太长」——改为全局默认朴素(不再逐页打补丁)

用户第三次提同一问题(地图 → 军官模块 → 同盟/军衔/附属野地),说明逐页挂 `.ezfy-plain-table`
是打地鼠。**直接改全局默认**:

```css
.ezfy-page table { width: auto; max-width: 100%; border-collapse: collapse; font-size: 15px; }
.ezfy-page table th,
.ezfy-page table td { border: 0; text-align: left; vertical-align: top; padding: 2px 10px 2px 0; }
.ezfy-page table th { color: #2f4156; font-weight: bold; white-space: nowrap; padding-bottom: 4px; }
```

实测(390px):军团表 165px、军衔表 153px、附属野地表 229px(原来都撑满 390),边框全 0,docW=390。

> 因为原版 `templates` 里几乎每张表都没有 CSS,所以「朴素」才是应该的默认值,
> 而不是需要显式声明的例外。模板里残留的 7 个 `.ezfy-plain-table` 类名是无副作用的 no-op。

### 12.2 军官二级导航字号

`.acade-tab` 由 14px 改 **16px**(窄屏 13px),与正文 `.old-line` 一致。
实测 390px:二级导航 13px = 正文 13px。

### 12.3 私聊(修复)

**后端本来就不要求好友** —— `POST /messages` 只拦「对方把我拉黑」。
缺的是**前端没有发信入口**(邮箱页只有收件箱)。

| 改动 | 说明 |
|---|---|
| 后端 `MessageHandler.Send` | 接收人支持 `to_name`(家园号码 → 昵称 → 唯一命中的模糊昵称);重名时提示「请用家园号码」 |
| 前端邮箱页 | 新增「发私信」面板:收件人输入框(`datalist` 候选 = 好友 + 军团成员 + 最近聊过的人,也可手填号码/昵称)+ 内容框 + 发送 |

实测:按号码发给**非好友** 10003 → `已发送给 咏荷`;按昵称发给闲云野鹤 → 成功;
不存在的玩家 → 404;发给自己 → 400;空收件人 → 400。

---

## 十三、第十九轮:军队表格 + 阵营兵种名丢失 + 统帅页精简(2026-09-19,用户反馈)

### 13.1 军队页表格

第十八轮的全局 table 改朴素后自动生效。实测:表宽 147px(原来撑满 390)、`td` 边框 0px、docW=390。

### 13.2 ★ 阵营兵种名全部丢失 —— 根因:种子脚本只解析了 INSERT

`ezfy_cfg_troop` 的 `name_axis` / `name_ally` **全为空**,`troopName(id, camp)` 因此永远回退通用名。

**根因**:`inithebing.sql` 里 `cfg_troop` 的 **INSERT 语句根本没有这两列**,它们是靠后面
**16 条独立 UPDATE** 补的:

```sql
UPDATE `cfg_troop` SET `name_axis`='国防军步兵', `name_ally`='同盟国步兵' WHERE `id`=1;
UPDATE `cfg_troop` SET `name_axis`='虎式重型坦克', `name_ally`='潘兴重型坦克' WHERE `id`=6;
...
```

而 `tmp/gen_*.py` 生成脚本**只解析 INSERT** → 这 16 条 UPDATE 全被漏掉。

修复:
1. `internal/seed/ezfy_cfg_gen.go` 兵种切片补上 16 个兵种的 `NameAxis` / `NameAlly`
2. `internal/seed/ezfy.go` 加一段**幂等对齐**(直接 UPDATE 两列),覆盖已有库
3. 当前库直接应用了那 16 条 UPDATE

实测(camp=1 同盟国):`同盟国步兵` / `摩托化侦察兵` / `通用运输卡车` / `装甲运兵车` /
`斯图亚特轻型坦克` / `潘兴重型坦克` / `牧师自行突击炮` / `BM-13 喀秋莎火箭炮` ✓

> ★ 教训:移植配置表**不能只抓 INSERT**,后面跟着的 UPDATE 往往才是关键字段
> (适用于 `cfg_building` / `cfg_tech` / `cfg_equipment` 等所有配置表)。

### 13.3 统帅页精简

按用户要求删掉 `ID / 等级 / 特权 / VIP等级 / 经验 / 状态` 这一块,
保留:账号 / 昵称 / 阵营 / 声望 / 军衔 / 城市数 / 人口数 / 军官数 / 总兵力 / 城外行进 / 占领野地。

> `ezfyConfigCache` 是 `sync.Once` 进程内缓存,改配置表后**必须重启后端**才生效。

---

## 十四、第二十轮:军团发言框缩短 + 地图居中(2026-09-19,用户反馈)

| 项 | 原来 | 现在 |
|---|---|---|
| 军团发言输入框 | `width:64%`(≈240px) | **`width:45%`**(实测 177px) |
| 地图格子间隔 | `border-spacing: 6px 2px` | **`8px 3px`**(窄屏 `5px 2px`) |
| 地图整表位置 | `margin: 6px 0`(靠左) | **`margin: 8px auto`**(水平居中) |
| 格子对齐 | `text-align: left` | **`text-align: center` + `vertical-align: middle`** |

实测:390px 下网格宽 289px,左间距 42 / 右间距 43(基本等分);320px 下左 7 / 右 8,docW=320 不溢出。

---

## 十五、第二十一轮:活动野地 / 活动寇城 / 特殊城市(E2,2026-09-19)

清单里最后一块大的未完成功能。原版位置:`GameServiceImpl.isActivityWildland / isActivityKouCity /
isSpecialCity / getActivityLevel / buildActivityDefender / processActivityBattle`,
格子标记在 `MapController`(`actWild / actKou / actCity`),文案在 `templates/activity/activityIndex.html`。

### 15.1 坐标哈希判定(与普通野地/寇城互斥)

| 目标 | 哈希 | 额外条件 |
|---|---|---|
| 活动野地 | `h = abs(x*1729 ^ y*3803)`,`h % 499 == 0` | 距 (250,250) 曼哈顿距离 > 100,**且非寇城** |
| 活动寇城 | `h = abs(x*9157 ^ y*2657)`,`h % 1499 == 0` | 距离 > 120,**且必须是寇城** |
| 特殊城市 | `h = abs(x*6689 ^ y*5347)`,`h % 2999 == 0` | 距离 > 150,**且非寇城、非活动野地** |
| 活动等级 | `h = abs(x*817 ^ y*1013)`,`h % 3 + 1` | 1~3 级 |

复刻 `MapController` 的互斥顺序:`actKou = isKou && 活动寇` → `actWild = !isKou && 活动野地`
→ `actCity = !isKou && !actWild && 特殊城市`。全部**不落库**,纯坐标推导,全服一致。

实测分布(0~500 全图 / 其中城市活动区 50~450):
活动野地 **465 / 266**,活动寇城 **5 / 2**,特殊城市 **67 / 40**。

### 15.2 守军与奖励(`buildActivityDefender` / `processActivityBattle`)

守军按「基础值 × 等级」,海洋格子(地形 8)换成海军:

| 类型 | 基础值 | 兵种配比(陆 / 海) |
|---|---|---|
| 活动野地 | 10 万 × Lv | 装甲车50/轻坦30/重坦20 · 驱逐40/潜艇30/战列20/航母10 |
| 活动寇城 | 20 万 × Lv | 同上 |
| 特殊城市 | 100 万 × Lv(**Lv3 额外 ×5**) | 同上 |

奖励(胜利):资源 `{10万,25万,60万}[Lv-1]` ×(寇城 2 / 特殊 5),入城上限放宽到 **`资源上限 × 3`**;
黄金 `(5000/10000/20000) × Lv`;`wildlandLoot(Lv*3, 地形, special=true)` 必掉宝物;
声望 `200 × Lv × (1/3/5)`。**打赢不占领、不占附属野地上限**。

### 15.3 后端改动

- **新增 `server/internal/handler/ezfy_activity_target.go`**:哈希判定 4 个纯函数 + `ezfyActTypeFor`
  + `ezfyActTargetType`(方法版,内部查寇城)+ 守军/奖励 5 个辅助 + `ezfyActWildlandView`(详情)
  + `processActivityBattle`(独立战斗结算)。
- `ezfy_order.go` `MapView`:格子加 `act_type` / `act_level` / `act_name`,非城市格子统一走
  `kou := ezfyIsKouCity()` 一次查库(原来 `terrain==8` 与 `isKou` 各判一次),再打活动标记。
- `ezfy_order.go` `WildlandView`:命中活动目标直接返回 `ezfyActWildlandView`,不走 `ezfy_cfg_wildland`。
- `ezfy_order.go` `processArrive`:**掠夺/征服**先判活动目标 → `processActivityBattle` 提前返回;
  **侦查**在 `case 1,2` 里换成活动守军回报情报。
- `troopChangeText` 加 `sort.Ints(ids)`:原来战报里兵种顺序随 Go map 随机遍历而变,现在固定按兵种 id 排。

> 有意偏离原版一处:原版 `processArrive` 里活动判定在订单类型判断**之前**,导致「侦查」活动目标也会
> 直接打一场。本项目把侦查改成正常回报活动守军情报(更符合「侦查=情报」的设计意图)。

### 15.4 前端改动(`Ezfy.vue`)

| 位置 | 改动 |
|---|---|
| `cellText` | 活动野地 → `活动(N)`,活动寇城 → `活动寇(N)`,特殊城市 → `特殊(N)` |
| `cellClass` | 新增 `ezfy-act-wild`(#ff6600)/ `ezfy-act-kou`(#ff00ff)/ `ezfy-act-city`(#d00000) |
| 地图图例 | 补一行活动目标说明 |
| 目标详情(`wildview`) | `selDetail.act_type` 时走活动分支:说明 + 守军明细 + 总兵力 + 奖励预览 + 宝石 |
| 目标详情操作行 | 活动目标不再显示「(占领该野地后可采集)」 |
| 活动页(`activity`) | 按 `activityIndex.html` 补【活动玩法】三大目标说明块,与【节日活动】分区 |

### 15.5 本轮验证(纯 DOM 断言 + 接口实测,无截图)

- 接口:地图 (328,62) r=8 命中 `活动野地 L1 @(323,69)`;详情返回 `act_type/act_level/act_name/act_desc/act_total/gold/prestige`。
- 战斗:征服活动野地 1 级(10 万守军)——25 回合胜,重坦 100000→77156,得粮钢油稀各 10 万 + 黄金 5000
  + 高级宝物「合金装甲」+ 声望 200 + 伤兵入营 2741;战报标题 `活动野地1级战斗报告: 活动野地`。
- 战败:掠夺特殊城市 3 级(500 万守军)派 1 步兵 → `我军战败!`,订单 `status=4`,无奖励。
- 侦查活动野地 → `侦查报告: 活动野地1级`,守军 `装甲车×50000 轻型坦克×30000 重型坦克×20000`。
- 前端:活动野地/特殊城市/活动寇城 三种格子文案与配色正确;320px 溢出扫描 22 页全 ok;全站回归前端错误 **0**。

---

## 十六、第二十二轮:联络中心/兵种详情/训练页/城防页 + 地图对齐(2026-09-19,用户反馈)

### 16.1 地图:表格靠左,内容居中(改回)

上一轮把整张表 `margin: 8px auto` 居中了,理解错了 —— 用户要的是**表格里的内容**居中。

```css
.ezfy-page .ezfy-map-table { margin: 8px 0; }      /* 8px auto → 8px 0: 表格靠左 */
.ezfy-page .ezfy-map-table td {
  text-align: center; vertical-align: middle;       /* 这两条保留 = 内容居中/垂直居中 */
}
```
实测 390px:表格 left=10 / 父容器 left=8(靠左),td `align=center / valign=middle`。

### 16.2 联络中心页(复刻 `liaison/liaisonIndex.html`)

`cur='liaison'`(军事区点「联络中心」建筑进入,`bEntry[15]`)。按参考页重排:

| 区块 | 内容 |
|---|---|
| 标题 | `联络中心（N级）` |
| 说明 | 「联络中心是盟友间互相联络的建筑 / 1级可以加入联盟 / 2级可以创建联盟 / 创建联盟需消耗X黄金（原版为 50 钻石） / 每级多一支盟友驻军、多10人联盟人数上限」 |
| 我的联盟 | **有军团 → 显示军团名 + (成员x/y) + [进入军团] + 公告**;**没有 → `加入联盟` `创建联盟` 两个链接** |
| 盟军驻军 | 来自城市 / 军官 / 驻军 表格 + `驻军上限 used/cap` |

> 原版 Java 这一页是**纯静态文案**(`任务.md` 也标未完成),本项目按文案把规则真正实现了。

### 16.3 兵种详情页(复刻 `city/cityTroopView.html`)

新增 `cur='troopview'`。入口:**军队页 / 军工厂页 / 城防页 / 训练页**里点兵种名。

字段全部按参考页两列表格:粮食/钢铁/石油/稀矿/耗时/油耗/耗粮/人口/生命/对地/对空/对海/对防/速度/军种/射程/攻速/负重/防御(+修复率),
底部「军工厂: N级 / 前提: …」+ `[训练]`/`[建造]` + 二级导航 + `[返回]`。

后端:不新增接口,把 `GET /games/ezfy/troops` 的 `cfgs[]` 补上
`atk_sea/atk_ground/atk_air/atk_def/food_keep/oil_keep/icon/repair_rate` 和
`need_factory`(从 `require` 文本里解析「军工厂(N级)」的 N)。

### 16.4 训练确认页(复刻 `city/createTroop.html` / `city/createDefence.html`)

新增 `cur='trainpre'`,同一个页面对两种模式(`trainMode = troop | defence`):

```
[军事区 ->军工厂(N级)：]  或  [【城防建造】城防空间:used/cap]
<兵种>训练需求：
粮食：50 / 钢铁：70 / 石油：10 / 稀矿：100
人口：1 / 吃粮：1 / 时间：1分0秒 / 需要军工厂：1级
建造数量：[____] (最多 N)
操作选项：(o)[全部工厂] ( )[仅此工厂]
预计耗时：…
[开始训练 / 开始建造]
```
- 「最多 N」= 资源 / 人口(城防为城防空间) 取最小;**N=0 时给红字提示 + 快捷入口**。
- 原版「[全部工厂]/[仅此工厂]」= 本项目的「分批(多军工厂同时训练)」,改成单选按钮。

### 16.5 城防页(复刻 `city/troopDefence.html`)

| 原来 | 现在 |
|---|---|
| 每个城防列全部属性 + 现有城防清单 | `围墙：N级 城防空间：(used/cap)` + `正在建造:` 队列 + **表格:`兵种:` / 数量 / `[建造][拆除]`** |

- 后端 `GET /troops` 新增 `defence_space`(围墙容量) / `defence_space_used` / `factory_total` / `factory_count`。
- **新增接口** `POST /games/ezfy/troops/dismiss` —— 拆除城防设施(释放城防空间)。
  原版 Java 没有这个接口,模板里的 `[拆除]` 是指向旧 PHP 站点的失效链接,本项目补成真接口。
- 参考页的 `<img>` 图标列**未复刻**:项目里没有 `assets/img/17-20.png` 这些图,留空比放裂图好。

### 16.6 页面导航

新增 `.ezfy-subnav` 样式(小一号),复刻原版军队/城防/兵种页里的二级导航行
「资源 · 军官 · 军队 · 科技 · 城防 · 统帅」,已挂到 军队/训练/城防/兵种详情/训练确认/军工厂 六页。

### 16.7 明确不复刻的三样

按用户要求,**新复刻的页面都不带**参考模板底部那三样:
`WAP报时:` / `联系我们：` / `<hr/>` 下划线。
实测 9 个页面全部无 `WAP报时`、无 `联系我们`、`.ezfy-page hr` 计数为 0。

### 16.8 本轮验证(纯 DOM 断言,无截图)

- 地图:表格靠左(表格 left 与父容器 left 差 2px),td `align=center/valign=middle`。
- 军事区点「联络中心」→ `cur=liaison`,文案与参考一致;有军团显示 `测试联盟A(成员3/20)`,
  临时移出军团后正确显示 `加入联盟 / 创建联盟`(验证完已恢复)。
- 军队页/军工厂页点「同盟国步兵」→ `cur=troopview`,20 行字段全部渲染。
- 军工厂页点 `[训练]` / 城防页点 `[建造]` → `cur=trainpre`,两种模式头部文案各自正确。
- 城防页:表格 4 行 `碉堡/榴弹炮/反坦克炮/防空炮`,各带 `[建造][拆除]`。
- 接口实测:`POST /troops/dismiss`(碉堡×7)→「已拆除 碉堡×7, 城防空间已释放」;
  拆非城防兵种 →「该兵种不是城防设施」。
- 320px 溢出扫描:22 个主页面 + 4 个新页面全部 ok;全站回归前端错误 **0**;`go vet ./...` 通过。

---

## 十七、待完成功能清单

### A. 军官系统剩余(接上本轮)

| # | 功能 | 原版位置 | 说明 | 优先级 |
|---|------|---------|------|-------|
| A9 | 装备钻石购买(`mall/buyDiamond`) | `MallController` | 依赖充值体系 | 低 |

### B. 社交

| # | 功能 | 原版位置 | 现状 | 优先级 |
|---|------|---------|------|-------|
| B1 | 好友搜索(按号码/昵称) | bug 清单 #17 | ✅ 已实现 | — |
| B2 | 聊天频道(公共/军团/系统/私聊) | `/ezfy/chatB?type=1/2/4` | ✅ 已实现 | — |
| B3 | 联络中心(盟友驻军) | `/ezfy/liaison/index` | ✅ 已实现(原版仅静态文案) | — |
| B4 | 查看他人统帅信息 | `/ezfy/infoOther` | 未实现(聊天/好友点玩家名进主页) | 低 |
| B5 | 军团邮件群发(`corps/mail`) | `CorpsController` | 未实现 | 低 |

### C. 道具 / 商城 / 充值

| # | 功能 | 原版位置 | 现状 | 优先级 |
|---|------|---------|------|-------|
| C1 | 批量使用 / 批量购买 | bug 清单 #8 | ✅ 已实现 | — |
| C2 | 招生简章 / 经验书 / 军官技能书 / 重修书 | 道具 #7/#8/#9 | ✅ 已实现 | — |
| C3 | 集结令 / 信号弹 / 随迁 / 高迁 | 活动礼包明细 | 未实现(城市迁移本身也未做) | 低 |
| C4 | 充值 / VIP | `/ezfy/welfare/vip` | 未实现 | 低 |
| C5 | 将领宝箱(随机/指定 1-5 星) | 道具 #9 | 未实现(军官已可俘虏/招募,宝箱为补充渠道) | 低 |
| C6 | 珠宝类宝物按地形掉落 | 道具 #10 | ✅ 已实现(随野地掉宝) | — |

### D. 战斗 / 城防 / 战报

| # | 功能 | 原版位置 | 现状 | 优先级 |
|---|------|---------|------|-------|
| D1 | 仓库保护资源(掠夺不触碰仓库存量) | 建筑升级一览·仓库 | ✅ 已实现(含 4 项资源保护配比) | — |
| D2 | 城防战报 / 军队动态详情(`viewCityTroopOut`) | `ReportController` | ✅ 已实现(命令详情页) | — |
| D3 | 战斗动画 / 回合详情页 | `battle.html` | 仅文字战报(逐回合详情已可展开) | 低 |
| D4 | 指挥中显示对方兵力/军官 | bug 清单 #19 | 侦查报告已有,指挥页无 | 低 |
| D5 | 时间显示精确到时分秒 | bug 清单 #20 | ✅ 已修(含秒) | — |

### E. 活动

| # | 功能 | 原版位置 | 现状 | 优先级 |
|---|------|---------|------|-------|
| E1 | 节日活动(增产 / 造兵打折等) | `福利.txt` | ✅ 已实现(5 种类型可配置,管理端开关) | — |
| E2 | 活动野地 / 活动寇城 / 特殊城市 | `活动野地.txt` / `activityIndex.html` | ✅ **已实现(第二十一轮)** —— 坐标哈希固定生成,守军 10万~500万,打赢结算资源/黄金/必掉宝物/声望,不占领 | — |

### F. 市政 / 建筑子页(原版独立页,本项目已内联的标注为已覆盖)

| # | 原版路由 | 现状 | 优先级 |
|---|---------|------|-------|
| F1 | `/ezfy/cityHall/sourceSet`(资源分配) | ✅ 原版是静态提示页;本项目以「仓库调配」实现其真实意图(4 项资源保护配比);另按原版页面补了「调整生产(开工率)」 | — |
| F2 | `/ezfy/cityHall/buildingList`(建筑列表) | 已由「市政厅」页总览覆盖 | ✅ |
| F3 | `/ezfy/cityHall/citySourceList` / `cityTroopList` | 未实现独立页 | 低 |
| — | `map/mapView`(野地详情页) | ✅ 已实现(第八轮,`cur=wildview`) | — |
| F4 | `/ezfy/cityHall/cityMove`(城市迁移) | ✅ 已实现(迁城计划/高级迁城/沿海迁城,20 万黄金) | — |
| F5 | `/ezfy/cityTroop/factory`(军工厂) | ✅ 已实现(第八轮) | — |
| F6 | `/ezfy/cityTroop/tech`(兵种科技) | 未实现独立页 | 低 |
| F7 | `/ezfy/map/mapAreaOrders`(地图区域命令) | 已由「出征队列」页覆盖 | ✅ |
| F8 | `/ezfy/resourceA`、`/ezfy/cityFactory` | 旧版遗留页,可不做 | — |

### G. 已确认已复刻(无需再动)

账号登录注册 · 城市系统(资源/人口/民心/民怨/税率/召集/安抚) · 建筑 21 种 1-10 级(建造/升级/拆除/一键满级) · 兵种 20 种训练 + 城防 4 种 · 科技 21 种 1-10 级 · 地图(9×5 视野/平移/回城/野地等级) · 七类出征(侦查/掠夺/征服/采集/运输/增援/派遣) · 回合制战斗引擎 · 战报(侦查/掠夺/征服/守卫/城破/预警) · 军团(创建/加入/退出/踢人/公告/聊天/解散) · 军衔 20 级 + 声望/兵力排行 · 任务(15 条) · 每日签到 + 新手/每周/等级礼包 · 交易所挂单买卖 · 商城/背包 · 公告/通知 · 司令部兵种战斗配置 · 伤兵营/逃兵营 · 雷达站分级预警

---

## 十七、按步复刻建议顺序

1. ~~**A1→A3**(出征带将 + 军官入战斗)~~ —— **已完成(第二轮)**
2. ~~**D1 仓库保护** + **D2 军队动态详情**~~ —— **已完成(第三轮)**
3. ~~**B1 好友搜索** + **B2 聊天频道** + **B3 联络中心**~~ —— **已完成(第四轮)**
4. ~~**C2 军官类道具** + **C1 批量使用/购买**~~ —— **已完成(第五轮)**
5. ~~**F4 城市迁移**~~(第六轮) + ~~**E1 节日活动**~~(第七轮) —— 均已完成。
6. ~~**军情页三区改造**(军队动态/军情警讯/战斗报告)~~ —— **已完成(第九轮)**
7. ~~**军情页三区改造**~~(第九轮) + ~~**地图/详情/出征三页对齐**~~(第十轮) —— 已完成。
8. ~~**E2 活动野地 / 活动寇城 / 特殊城市**~~ —— **已完成(第二十一轮)**
9. 收尾项(均为低优先级,可按需挑):**B4 查看他人统帅**、**B5 军团邮件群发**、**F3 资源/军队总览页**、
   **F6 兵种科技独立页**、**D3 战斗动画**、**D4 指挥页显示对方兵力**、**C5 将领宝箱**、**C3 集结令/迁城道具**、**C4 充值 VIP**。
10. **Linux / Windows 部署包重新打包**(线上 `http://39.105.151.141:8080` 需更新到最新代码)。

---

## 十八、备注

- 原版 84 个模板中,有 73 个页面统一了 6 链接顶部导航(聊天/邮箱/军情/任务/好友/首页),本项目 `Ezfy.vue` 的 `.top-nav` 已统一。
- 原版为多页跳转(每个功能一个 URL),本项目为单页 `cur` 状态切换,视觉与信息结构保持一致,交互更顺。
- 原版部分页面含硬编码假数据(如 `headQuarters`/`factory` 模板),本项目司令部页为真实数据实现。
